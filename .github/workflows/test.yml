name: Web Security CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run Dummy Log Test
      run: echo "Test" > test.log && python main.py -l test.log
